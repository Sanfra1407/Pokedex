/**
 * @enum FILTER_TYPES
 * @description Define an enum which contains the types of search that users can do.
 */
enum FILTER_TYPES {
  name = "name",
  type = "type"
}

export default FILTER_TYPES;
